<button type="button" class="btn btn-default {{elementClass}} {{visibilityClass}}" on:click="back()">
    <i class="{{iconClass}} {{iconNameClass}}{{#if text}} {{iconSpacingClass}}{{/if}}" aria-hidden="true"></i>{{text}}
</button>
