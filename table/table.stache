<div class="{{containerClass}}">
    {{#if shouldShowEmptyMessage}}
        <div class="alert alert-info" role="alert">{{emptyMessage}}</div>
    {{else}}
        <table class="table {{tableClass}}">
            <thead>
            <tr class="row">
                {{#each(columns)}}
                    <th class="{{columnClass}} {{headerClass}}">
                        {{columnTitle}}
                    </th>
                {{/each}}
            </tr>
            </thead>
            <tbody>
            {{#each(rows)}}
                <tr class="row {{scope.root.getRowClass (.)}}" on:click="scope.root._rowClick(.)">
                    {{#each(../columns)}}
                        <td data-title="{{columnTitle}}" class="{{columnClass}} {{dataClass}}">
                            {{#if(hasStache)}}
                                {{scope.root.getView(../., .}}
                            {{else}}
                                {{scope.root.getColumnValue(../., .)}}
                            {{/if}}
                        </td>
                    {{/each}}
                </tr>
            {{/each}}
            </tbody>
        </table>
    {{/if}}
</div>